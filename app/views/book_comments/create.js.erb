$("#book_<%= @book.id %> .commens-count").html("コメント数:<%= @book.book_comments.count %>");
$('#comments_area').html("<%= j(render 'book_comments/comment_area', book: @book) %>");
$("textarea").val("");